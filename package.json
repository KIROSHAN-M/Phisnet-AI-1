{
  "name": "phishnet-ai-unified",
  "version": "1.0.0",
  "description": "Unified PhishNet AI Platform",
  "scripts": {
    "postinstall": "cd frontend && npm install && cd .. && pip install -r backend/requirements.txt",
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && uvicorn main:app --reload --port 8000\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
